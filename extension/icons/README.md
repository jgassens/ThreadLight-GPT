# Icons

Generated WebExtension manifest icons live here. They use the ThreadLight talk-bubble mark on a transparent canvas so Safari toolbar surfaces do not show a square background.

Regenerate them with `npm run build:safari` or `npm run sync:native`; the generator uses ImageMagick via `magick` or `MAGICK_BIN`.
